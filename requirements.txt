# Core data validation
pydantic==2.10.4

# ML
scikit-learn==1.6.1
torch==2.13.0
numpy
pandas

# MITRE ATT&CK (optional - rule engine works without it via fallback table)
mitreattack-python>=5.0.0

# Artifact generation
jinja2==3.1.6
pyyaml

# Database
# (sqlite3 is built into Python - no package needed)

# Terminal UI (choose one or both)
rich>=13.0
colorama

# Testing
pytest==9.0.3
pytest-asyncio

# Optional: keep if you want model experiment tracking
# mlflow==2.19.0
