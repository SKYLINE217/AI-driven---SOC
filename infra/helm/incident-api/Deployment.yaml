apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-api
  labels:
    app: incident-api
    version: "0.4.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: incident-api
  template:
    metadata:
      labels:
        app: incident-api
    spec:
      containers:
        - name: incident-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: soc-triager-secrets
                  key: database-url
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: soc-triager-secrets
                  key: anthropic-api-key
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: soc-triager-secrets
                  key: redis-url
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: soc-triager-jwt-keys
                  key: private-key-pem
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: soc-triager-jwt-keys
                  key: public-key-pem
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
