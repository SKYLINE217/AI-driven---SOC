# Intentionally empty — marks this directory as a Python package.
# This package provides the legacy in-memory store API used by the original
# test suite (test_incident_service.py). The production code uses the
# SQLite-backed services.incident_service instead.
