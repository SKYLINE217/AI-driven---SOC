---
# ============================================================
# Containment Playbook: Generic IOC Block (Fallback)
# DRAFT ONLY — Requires Approver authorization before execution
# Generated by SOC Triager · Incident: {{ incident_id }}
# Technique: {{ technique_id }}
# ============================================================
- name: "SOC Containment — Generic IOC Block ({{ technique_id }})"
  hosts: firewall
  become: yes
  vars:
    attacker_ip: "{{ attacker_ip }}"
    incident_id: "{{ incident_id }}"
  tasks:
    - name: Block attacker IP
      iptables:
        chain: INPUT
        source: "{{ attacker_ip }}"
        jump: DROP
        comment: "SOC-AUTO: {{ incident_id }} {{ technique_id }}"

    - name: Save iptables rules
      command: netfilter-persistent save
      ignore_errors: yes
